groundtruth/data/verdicts.json
# Downloaded/vendored data — not our IP, reproducible via the URLs documented in HANDOFF.md
data/
pyhealth_src/

# Copyrighted paper text — working extractions for internal analysis only, not for redistribution
*.pdf
paper.txt
paper.html

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Harness/local state
.claude/

# Third-party clones (external repos checked out for inspection, not our content)
confsleepnet/repo/

# Superseded local drafts from earlier non-canonical logbook attempts
.trackio_noncanonical_bak/
.trackio_failed/

# trackio's own branding assets, auto-copied into every logbook — not our content, pure duplication
**/.trackio/logbook/trackio-logo*.png
**/.trackio/logbook/trackio-wordmark*.png

# The challenge's own reference datasets — public on the HF dataset, no need to duplicate here
/papers.json
/claims.json
/claims_anchored.json
/all_papers.csv
/medical_neuro_papers.csv

# OS
.DS_Store
Thumbs.db

# trackio publish venv (local only)
.venv-trackio/

# verbatim paper excerpts (copyright, like paper.txt)
**/SPEC_notes.txt
experiments/**/official_code/
experiments/**/.venv/

# official paper code (nested git repo, read-only, do not commit)
experiments/run03_hermes_leaf_pilot/*/code_mdcp/
**/code_mdcp
**/code_mdcp/**
**/data/openml/**

# python venvs
**/.venv/
# large datasets / model artifacts
**/*.arff
**/*.arff.gz
**/data/openml/**

# ===== Repo hygiene (added 2026-08-03 reorg) =====
# Nested git repos (external clones) — never commit, causes gitlink bloat
**/.git/
**/.git/**
# Trackio PUBLISH bundles are regenerated from logbook.md via local_to_trackio.py.
# Keep only the source-of-truth (logbook.json + pages/*.md); ignore the static site.
**/.trackio/logbook/*.html
**/.trackio/logbook/*.js
**/.trackio/logbook/*.css
**/.trackio/logbook/*.svg
**/.trackio/logbook/*.png
**/.trackio/logbook/README.md
**/.trackio/metadata.json
# (logbook.json and logbook/pages/*.md remain tracked — they are the reproducible artifact)
# Paper source text (copyright) + downloaded HTML — already *.pdf ignored; add html forms
**/paper*.html
**/paper*.htm
**/paper/**.html
**/paper/**.htm
**/arxiv*.html
**/full*.html
**/*_paper.html
**/results/paper.*
**/h.html
**/p.html
# Groundtruth generated dumps (regenerated by process_gt.py) — keep scripts, drop outputs
groundtruth/out/
# Large/third-party data dirs (reproducible via documented URLs, local-only per skill rules)
**/data/
experiments/**/data/
# Stray third-party clones inside runs
**/eoss_repo/
**/code_mdcp/
# OS / editor
.DS_Store
Thumbs.db
*.swp
