# SPROUT static reproduction logbook

Build from the candidate directory:

```powershell
.\.venv\Scripts\python.exe repro_sprout/build_logbook.py
```

The generated `.trackio` directory is intentionally committed as the static
publication artifact.
