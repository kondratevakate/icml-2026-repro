# Handoff — medical/relevant clean pipeline

Дата остановки: 2026-07-30

Ветка: `codex/medical-reproducibility-map`

## Состояние на остановке

Работа намеренно остановлена после полного локального закрытия DPsurv. Новая
статья после него не начиналась.

Последние завершенные пакеты:

| Commit | Пакет | Локальный результат |
|---|---|---:|
| `bbb4754` | SPROUT (`qYfNhYenuu`) | 5/12 |
| `f75e431` | MedCRP-CL (`v0DWbfP3b9`) | 4/10 |
| `4026444` | Bayesian causal meta-learning (`k76ll7aQyE`) | см. пакет |
| `643b054` | DC-PnPDP (`jEBkuuETjr`) | см. пакет |
| `ad75602` | GLEAN | пакет и clinical-data audit |

DPsurv должен быть следующим commit после `bbb4754`.

## DPsurv

Папка: `dpsurv/`

Paper: `RKqL4GYXz3`, arXiv `2510.00053`

Frozen forecast: 5/12

Финальный подготовленный результат: **4/12**

Распределение:

- C1 five-cohort protocol: 1/2;
- C2 dual-prototype evidential fusion: 2/2;
- C3 GRFN bounds/mixture: 1/2;
- C4 headline discrimination: 0/2;
- C5 calibration/uncertainty: 0/2;
- C6 interpretability/ablations/robustness: 0/2.

Ключевые независимые результаты:

- 5 cohorts, 25 outer folds, 50 CSV;
- ни в одном fold нет case/slide train-test overlap;
- каждый case и slide попадает в test ровно один раз;
- 86 уникальных строк не имеют DSS endpoint и фильтруются trainer;
- synthetic two-component/two-prototype forward, loss, backward и 12/12
  gradient tensors конечны;
- при `lambda=0.5` paper/training/evaluation совпадают;
- вне 0.5 training использует lambda дважды, evaluation меняет местами
  направление Bel/Pl;
- при `lambda=0.75` training survival достигает 1.414;
- all-low-prevalence component дает 0 prototypes и
  `RuntimeError: stack expects a non-empty TensorList`;
- второй `get_panther_encoder` затеняет первый и вызывает неопределенный
  `create_embedding_model`;
- run-level TCGA features/checkpoints/predictions/metrics/logs отсутствуют.

Preflight:

- 12 deterministic tests pass;
- 30 official Python files compile;
- PDF pages 5, 6, 8, 16 rendered and visually checked;
- local Trackio logbook validator passes;
- intended Space: `kondratevakate/repro-dpsurv-release-audit`.

## Датасеты

### MIMIC-IV-Ext CDM v1.1

Уже размещен:

- payload: `data/mimic-iv-ext-cdm-1.1/`;
- archive: `data/archives/mimic-iv-ext-cdm-1.1.zip`;
- archive bytes: `3,820,629`;
- archive SHA-256:
  `c55ad01aaf9d424a582c404687730614a78a0f4155a5889b3c7108fab5fe038b`;
- 12/12 payload hashes match the official `SHA256SUMS.txt`;
- 2400 cases: appendicitis 957, cholecystitis 648, pancreatitis 538,
  diverticulitis 257.

Этот набор уже использован GLEAN (`ad75602`). Повторно скачивать или
распаковывать его не надо.

### MIMIC-IV-Note v2.2

Уже распакован в `data/mimic-iv-note-2.2/`. Не создавать вторую копию.

### Активные загрузки

В `C:\Users\kondr\Downloads` на момент handoff:

- `Unconfirmed 369833.crdownload`: около 4.29 GB — MIMIC-IV v3.1;
- `Unconfirmed 493643.crdownload`: около 0.91 GB — MIMIC-IV ECG.

Не перемещать и не переименовывать `.crdownload`. Дождаться завершения Chrome,
проверить конечные имена и размеры, затем перенести архивы в `data/archives/`
и распаковать в отдельные ignored data-папки.

## Публикация

Новые remote Spaces не создавались: ранее подтвержден дневной лимит Hugging
Face 20 Spaces и HTTP 429 с рекомендацией повторить примерно через 24 часа.
Локальные logbooks валидны. После снятия лимита публиковать только после
повторного local validator и проверять remote artifact hashes.

## Безопасная точка продолжения

1. Проверить `git status`; в worktree много старых пользовательских изменений,
   их не включать в новые commits.
2. Проверить завершение двух `.crdownload`; не трогать частичные файлы.
3. Когда MIMIC-IV v3.1 завершится, он открывает полноценные MLCI/PyHealth
   cohort checks.
4. Когда MIMIC-IV ECG завершится, вернуться к ECG-R1/LVCG paths.
5. Если продолжать до данных, следующий независимый medical candidate —
   PathCTM (`LgFmen6sHP`), но у него нет released multi-scale features или
   checkpoints; сначала делать строгий go/no-go, а не обещать empirical score.
6. Сохранять pipeline:
   `shortlist -> paper/source -> official artifacts -> frozen forecast ->
   independent checks -> local validator -> scoped commit -> push`.

## Команды проверки DPsurv

```powershell
cd C:\Projects\02_academia\icml-repro\dpsurv
.\.venv\Scripts\python.exe audit_dpsurv.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe repro_dpsurv\build_logbook.py
.\.venv\Scripts\python.exe ..\validate_icml_logbook.py
```
