# Solvable AE reproduction bundle

Rerun:
    python verify_solvable_ae.py --smoke-only
    python verify_solvable_ae.py --d 800 --alphas 15 30 --instances 2 --epochs 800 --kstars 2 3

numpy only, CPU, synthetic (no external data).
