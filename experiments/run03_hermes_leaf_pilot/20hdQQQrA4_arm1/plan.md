# plan.md — reproduction plan (CAffNet, 20hdQQQrA4)

Environment: `.venv` (py3.12, numpy/scipy/sympy/torch-CPU). All work CPU-only, inside this directory.

| # | Claim (short) | Type | CPU feasible? | Approach |
|---|---|---|---|---|
| 1 | Thm 3.5 UAT with error bound (3+3*sqrt(n_out))K | theory (analytic, numerically checkable) | YES | Implement Eq.(8)/(9)/(12) exactly; construct instances satisfying the proof's hypotheses (\|\|f_theta-f_t\|\|_p<K, \|\|w_phi\|\|_p<2K, f_t feasible); check \|\|P*-f_t\|\|_p < (3+3 sqrt(n_out))K over an exhaustive grid of (n_out, m, p, rank-pattern, seed). Also check the intermediate bounds (23),(28),(31),(1+3sqrt n)K,(2+3sqrt n)K. Mutation: (a) violate the null-space premise (\|\|w_phi\|\| >> 2K), (b) replace argmin selection in (12) by argmax — bound must break. |
| 2 | Eq.(8) has trainable null-space term enabling joint optimisation over feasible projections (vs fixed orthogonal projection) | theory + tiny simulation | YES | Exact algebra: (i) A_g P_g = b_g for arbitrary w_phi when the system is consistent; (ii) the map w_phi -> P_g is onto the whole affine solution set {y: A_g y = b_g} (so all feasible projections on a face are reachable); (iii) w_phi = 0 reproduces the orthogonal (min-norm-correction) projection; (iv) a small task where optimising w_phi gives strictly lower loss than w_phi=0. Mutation: delete the null-space term -> reachable set collapses to a single point and the achievable loss stays at the orthogonal-projection value. |
| 3 | No full-row-rank requirement; decomposition into sub-constraints of cardinality <= min(m,n_out) handles arbitrary m (Lemma 3.3) | simulation, exhaustive | YES | Exhaustive enumeration over a finite family of small constraint systems (incl. duplicated / linearly dependent / redundant rows, m>n_out) plus randomised sweeps: CAffNet candidate set must always contain a feasible point; a HardNet-Aff-style single pseudo-inverse correction must produce infeasible outputs on a nonzero fraction of dependent-row instances. Mutation: truncate Gamma to k <= min(m,n_out)-1 -> feasibility failures must appear. |
| 4 | 73.33% MSE reduction of CAffNet-TF vs soft-constrained NN with zero violations (Table 2, Sec 4.1) | simulation (training) | YES (small: n_in=n_out=1, 50 train samples, m=4 so \|Gamma\|=4) | Reimplement App. D.1 target + 4 piecewise bounds; train NN(soft), HardNet-Aff, CAffNet-FF, CAffNet-TF with the paper's hyperparameters (Adam 1e-4, 3x200 ReLU / 3-head-40 TF, 5 seeds). Report MSE, violations, and reduction %. Also arithmetic re-check of 1-0.0012/0.0045. Epoch count reduced only if the time budget forces it — this is recorded explicitly. Mutation: disable the CAffine layer in the TF model -> violations reappear. |
| 5 | Safety-critical control: CAffNet avoids obstacles, HardNet + soft NN fail (Table 4, Sec 4.3) | simulation (training, 300 initial states x 150 steps x epochs, differentiable rollout) | PARTIAL — expensive on CPU, and the outcome for the *baselines* is a training-dependent empirical fact | Implement the unicycle + smooth-union CBF constraints from App. D.3 exactly. Budget-limited: run a reduced-scale training (fewer initial states / epochs) and report as a scaled-down run, and additionally test the constraint-satisfaction mechanism (which is deterministic) on the paper's test initial state x0=[-4.5,0,0.5]. If the reduced scale cannot support the comparative claim, verdict = `toy` or `inconclusive` with the exact reason — NOT a fabricated match to 4.1411e5 / 7.2060e5. |

Priority order: 2, 3, 1 (pure/cheap), then 4 (training), then 5 (heaviest, capped at 2h).
No claim is marked `verified` without (a) more than one seed / exhaustive enumeration and (b) a mutation test.
