# notes_code.md

No official code or data release exists for this paper. Grep over the full paper text for
`github|huggingface|hf.co|available at|anonymous|release` returns only bibliography entries and one
third-party footnote (`https://github.com/hiyouga/LLaMA-Factory.git`, used for SFT training, not the benchmark).

Consequence: nothing to grep in an official repo; no scorer, no dataset, no model outputs.
The only machine-readable artifact of the paper is its own tables, parsed by `paper_tables.py`.
