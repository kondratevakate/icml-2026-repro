# plan.md

Start of work: see logbook.md timestamps. Paper read ONCE (v1 + v2 PDFs) → notes_paper.md.

| Claim | Content | Type | CPU-feasible? | Plan |
|---|---|---|---|---|
| 1 | ERT family (L1/L2/KL) built on "no classifier beats constant 1−α under perfect conditional coverage" (Table 1, Sec 3.1) | theory (analytic/symbolic) + numeric | YES | sympy/numpy: (a) prove each Table-1 score is proper (constant/true-p minimizes expected loss) on an exhaustive grid of (p, q); (b) verify each ℓ-ERT closed form equals E[d_ℓ(1−α,p)] exactly; (c) verify optimality of 1−α under p≡1−α. Mutation: replace proper score by an improper one (e.g. ℓ(p,y)=|p−y|) → constant 1−α no longer Bayes-optimal / ERT formula breaks. |
| 2 | LightGBM 68.4% vs PartitionWise 38.3% relative power on 8 TabArena regression datasets (Table 2) | data + heavy pipeline; exact number needs GPU baselines (TabICL / RealTabPFN define the max) | PARTIAL | Time-boxed ≤2h. Reproduce the pipeline shape on a few real CPU-feasible regression datasets: 40/10/50 split, S=|Y−f(X)|, α=0.1, 5-fold CV, LightGBM vs PartitionWise (k-means groups). Report the *ordering / relative gap*; exact 68.4/38.3 cannot be reproduced without GPU foundation models normalising the max and without the exact 8 TabArena datasets → expect `inconclusive` on the exact numbers with quantitative supporting evidence. NO fake toy substitute for the headline number. |
| 3 | Synthetic: CovGap unreliable/unaligned even at 5,000 test points; L1-ERT converges with far fewer (Figure 4, Sec 4.2) | simulation | YES | Full reproduction of the v1 DGP (X~U[−1,1]^8, Y~N(0,σ(X1))) with standard-CP vs oracle sets; sweep n_test ∈ {250…5000(+)}; compute CovGap, L1-ERT (5-fold CV, gradient-boosted classifier), and the ground-truth theoretical value from 300k oracle samples; ≥10 seeds each. Decision criterion from paper: at n=5000 CovGap gives *nearly identical* values in both scenarios and far from its theoretical target, while L1-ERT separates them and tracks the truth. Mutation: swap the ERT classifier for the PartitionWise/k-means classifier → L1-ERT should degrade to CovGap-like behaviour. |
| 4 | Asymmetric decomposition ℓ₊-ERT / ℓ₋-ERT (Sec 3.3) | theory | YES | Symbolic + exhaustive numeric check of ℓ(p,y)=ℓ₊+ℓ₋−ℓ(1−α,y), ERT = ℓ₊-ERT + ℓ₋-ERT, and one-sidedness (f₊=0 for p<1−α, f₋=0 for p>1−α), for L1/L2/KL. Mutation: swap max/min in the definitions → additivity/one-sidedness fails. |
| 5 | Classification KL₊/KL₋ divergent across CP methods (Table 4, Sec 4.3.2) | data + CNN training (4 datasets, 10 runs) | PARTIAL | Time-boxed ≤2h. MNIST/FashionMNIST CNN on CPU is feasible for 1 run; CIFAR100 ResNet is not within budget. Attempt MNIST (+FashionMNIST if time): train small CNN, build cumulative & likelihood conformal sets, compute L1/KL/KL₊/KL₋-ERT with Algorithm 1. Report as partial reproduction; exact Table-4 numbers for CIFAR10/100 → `inconclusive`. |
| 6 | Algorithm 1 = k-fold CV estimation to avoid overfitting the classifier (Algorithm 1) | simulation | YES | Implement Algorithm 1 verbatim; on data with *perfect conditional coverage* (Z ~ Bernoulli(1−α) independent of X) the CV estimate must be ≈0 while the no-CV (train=eval) estimate is strongly positive (overfitting). Multi-seed, several m. Mutation = the no-CV variant (that *is* the deliberate break of the mechanism). |

Priority order: 1, 4, 6, 3 (CPU-cheap, decisive) → 3 mutation → 2 → 5.
Hard stop 8h; per-claim cap 2h.
